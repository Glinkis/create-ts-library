import * as mocha from "mocha";
import * as chai from "chai";
import * as easing from "../../src/animation/easing";

const { describe, it } = mocha;
const { expect, assert } = chai;

describe("Test 1", () => {
  it("Expects something to be true", () => {
    assert(true);
  })
});
